{
  "name": "budgeted-ui",
  "version": "1.0.0",
  "description": "a gui for your budget data",
  "main": "main.js",
  "scripts": {
    "build-main": "tsc",
    "build-ui": "node esbuild.config.mjs",
    "watch": "tsc -w",
    "start": "npm run build-main && npm run build-ui && electron ./dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "politicker",
  "license": "MIT",
  "devDependencies": {
    "electron": "^28.1.0",
    "esbuild": "^0.19.10",
    "prettier": "^3.1.1",
    "typescript": "^5.3.3"
  }
}
